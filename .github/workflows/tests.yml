name: Test

on:
  push:
    branches:
      - tests

jobs:
  cypress:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Prepare test data and start the application
        run: ./scripts/start_in_test_mode.sh

      - name: Cypress run
        uses: cypress-io/github-action@v2
