const logger = require('../util/logger')

const updateCoursesAndTeacherFeedbackTargets = () => {
  logger.info('Starting to update courses and teacher feedback targets')
}

module.exports = updateCoursesAndTeacherFeedbackTargets
